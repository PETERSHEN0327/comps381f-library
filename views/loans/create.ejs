<!DOCTYPE html>
<html>
<head><meta charset="utf-8"><title>New Loan</title>
<link rel="stylesheet" href="/style.css"></head>
<body>
  <div class="wrap">
    <div class="card">
      <h2>New Loan</h2>
      <a href="/loans" class="btn btn-secondary">&laquo; Back</a>
      <form method="post" action="/loans" style="margin-top:20px;">
        <label>Book</label><br/>
        <select name="bookId" required>
          <% books.forEach(b => { %>
            <option value="<%= b._id %>"><%= b.title %> — <%= b.author %></option>
          <% }) %>
        </select><br/>
        <label>Borrower</label><br/><input name="borrower" required /><br/>
        <label>Due Date</label><br/><input type="date" name="dueDate" required /><br/><br/>
        <button type="submit" class="btn btn-primary">Create Loan</button>
      </form>
    </div>
  </div>
</body>
</html>
