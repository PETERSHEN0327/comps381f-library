<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Register • Library</title>
  <style>
    body { font-family: system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif; background:#f6f8fb; margin:0; }
    .wrap { min-height: 100vh; display:flex; align-items:center; justify-content:center; padding: 24px; }
    .card { width: 100%; max-width: 460px; background:#fff; border-radius:16px; box-shadow: 0 10px 25px rgba(0,0,0,.08); padding:28px 24px; }
    h2 { margin:0 0 16px; font-size:26px; }
    .muted { color:#666; margin:6px 0 18px; }
    label { display:block; font-size:14px; color:#333; margin:10px 0 6px; }
    input { width:100%; padding:12px 14px; border:1px solid #d9e1ec; border-radius:10px; font-size:15px; outline:none; }
    input:focus { border-color:#6aa0ff; box-shadow:0 0 0 3px rgba(106,160,255,.15); }
    .row { display:flex; gap:10px; align-items:center; margin-top:14px; }
    .btn { display:inline-block; padding:12px 16px; border-radius:10px; border:0; cursor:pointer; font-weight:600; text-decoration:none; text-align:center; }
    .btn-primary { background:#16a34a; color:#fff; }
    .btn-primary:hover { filter:brightness(.95); }
    .btn-ghost { background:#eef2ff; color:#1e40af; }
    .btn-ghost:hover { filter:brightness(.98); }
    .error { background:#fee2e2; color:#7f1d1d; border:1px solid #fecaca; padding:10px 12px; border-radius:10px; margin-bottom:12px; }
  </style>
</head>
<body>
  <div class="wrap">
    <div class="card">
      <h2>Create your account</h2>
      <p class="muted">Register as a normal user</p>

      <% if (error) { %><div class="error"><%= error %></div><% } %>

      <form method="post" action="/register">
        <label>Username</label>
        <input name="username" required />

        <label>Password</label>
        <input name="password" type="password" minlength="6" required />

        <label>Confirm password</label>
        <input name="confirm" type="password" minlength="6" required />

        <div class="row">
          <button class="btn btn-primary" type="submit">Create account</button>
          <a class="btn btn-ghost" href="/login">Back to sign in</a>
        </div>
      </form>
    </div>
  </div>
</body>
</html>
